#include <stdio.h>
#include <sys/shm.h>
#include "common.h"


int main() {
    // int shm_id = shmget(SHM_KEY, SHM_SIZE, IPC_CREAT | 0666);
    // if(shm_id == -1) {
    //     puts("Failed to shmget.");
    //     exit(1);
    // }

    

    return 0;
}